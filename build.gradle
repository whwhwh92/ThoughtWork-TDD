apply plugin: 'java'
apply plugin: 'idea'

repositories {
    jcenter()
}

idea.project {
    vcs = 'Git' 
}

dependencies {
    compile 'org.slf4j:slf4j-api:1.7.14'

    testCompile 'junit:junit:4.12'
}
